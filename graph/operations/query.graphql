query ListUsers ($filter: UserListFilter,$perPage: Int){
    users(filter: $filter,perPage: $perPage){
        users {
            name
            id
        }
        pagination {
            itemsPerPage
            totalItems
        }
    }
}

query ListPatients ($filter: PersonFilter, $page: Int, $limit: Int){
    listPatients(filter:$filter,page: $page,limit: $limit) {
        pagination {
            totalItems
            prevPage
            currentPage
            itemsPerPage
        }
        persons {
            ...PersonFields
            Age
        }
    }
}

query GetPatient ($id: ID!){
    getPatient(ID: $id) {
        ...PersonFields
        Age
    }
}

query ListPeople ($filter: UserListFilter,$perPage: Int) {
    users(filter: $filter,perPage: $perPage){
        users {
            name
            id
        }
        pagination {
            itemsPerPage
            totalItems
        }
    }
}

query ListPersonMedicalHistory ($ID: ID!,$filter: PersonMedicalHistoryFilter, $page: Int, $limit: Int){
    listPersonMedicalHistories(PersonID: $ID,filter:$filter,page: $page,limit: $limit) {
        pagination {
            ...Pagination
        }
        histories {
            ...PersonMedicalHistoryFields
        }
    }
}