type User {
    id: ID!
    name: String!
}
type UserList {
    users: [User!]!
    pagination: Pagination
}

input UserListFilter {
    id : ID
    name: String
}

extend type Query {
    users(filter: UserListFilter): UserList
}