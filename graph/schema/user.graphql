type User {
    id: ID!
    name: String!
}

input UserLogin {
    username: String!
    password: String
}

type UserAuthenticated {
    token: String!
    user: User!
}

extend type Query {
    users: [User!]!
}


extend type Mutation {
    loginUser(input: UserLogin!): User!
}
