type Error {
    string: String!
    status: Int!
}

type Mutation {
    sample(id: ID!): Error
    createUser(id: ID): User
}

type Query {
    sample(id: ID!): Error
}

type Subscription {
    users: User!
}
