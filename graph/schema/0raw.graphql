type Error {
    string: String!
    status: Int!
}

type Pagination {
    currentPage: Int
    prevPage: Int
    nextPage: Int
    totalItems: Int
    itemsPerPage: Int
}

type Mutation {
    error: Error
}

type Query {
    error: Error
}

type Subscription {
    error: Error!
}
